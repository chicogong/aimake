# Cloudflare Pages 项目配置 (可选)
# https://developers.cloudflare.com/pages/platform/wrangler/

name = "aimake-landing"
compatibility_date = "2024-01-01"

# Pages 项目配置
pages_build_output_dir = "website"

# 环境变量 (如需要)
# [vars]
# ENVIRONMENT = "production"

# 自定义域名 (在 CF Dashboard 配置)
# routes = [
#   { pattern = "aimake.cc", zone_name = "aimake.cc" },
#   { pattern = "www.aimake.cc", zone_name = "aimake.cc" }
# ]
