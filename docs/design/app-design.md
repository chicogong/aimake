# AIMake 移动端 APP 设计方案

> 原生体验的 AI 语音内容生成移动应用

---

## 📱 产品定位

### 核心价值

- **移动优先**：随时随地创作语音内容
- **离线能力**：本地处理 + 云端增强
- **原生体验**：流畅的移动端交互
- **跨平台**：iOS + Android 统一体验

### 目标用户

- 移动端内容创作者（播客主播、自媒体）
- 碎片时间创作者（通勤、旅行）
- 需要快速配音的视频创作者
- 有声书爱好者

---

## 🎯 技术方案选型

### 方案对比

| 方案             | 优势                                                   | 劣势                                | 推荐度     |
| ---------------- | ------------------------------------------------------ | ----------------------------------- | ---------- |
| **React Native** | ✅ 代码复用（与 Web 共享）<br>✅ 热更新<br>✅ 社区强大 | ⚠️ 性能不如原生<br>⚠️ 包体积较大    | ⭐⭐⭐⭐   |
| **Flutter**      | ✅ 高性能渲染<br>✅ 优秀的 UI 组件<br>✅ 单一代码库    | ⚠️ 学习成本<br>⚠️ 与 Web 不共享代码 | ⭐⭐⭐⭐⭐ |
| **原生开发**     | ✅ 最佳性能<br>✅ 完整平台特性                         | ❌ 开发成本高<br>❌ 两套代码库      | ⭐⭐⭐     |
| **PWA**          | ✅ 无需安装<br>✅ 开发简单                             | ❌ 功能受限<br>❌ iOS 支持差        | ⭐⭐       |

### 推荐方案：**Flutter**

**选择理由**：

1. **性能优先**：语音处理需要流畅的 UI 和快速响应
2. **原生体验**：Material Design + Cupertino 双主题
3. **独立演进**：移动端可独立于 Web 端优化
4. **长期投资**：Flutter 是 Google 主推的跨平台方案

---

## 🏗️ 技术架构

### 技术栈

```yaml
框架: Flutter 3.x
语言: Dart 3.x
状态管理: Riverpod / Bloc
本地存储: Hive / Drift (SQLite)
网络请求: Dio + Retrofit
音频处理: just_audio + audio_service
录音: record / flutter_sound
文件存储: path_provider + flutter_secure_storage
推送通知: firebase_messaging
分析: firebase_analytics + Sentry
```

### 架构分层

```
┌─────────────────────────────────────┐
│         Presentation Layer          │
│  (UI Components + State Management) │
├─────────────────────────────────────┤
│         Business Logic Layer        │
│    (Use Cases + Domain Models)      │
├─────────────────────────────────────┤
│           Data Layer                │
│  (Repositories + Data Sources)      │
├─────────────────────────────────────┤
│        Infrastructure Layer         │
│  (API Client + Local DB + Cache)    │
└─────────────────────────────────────┘
```

### 目录结构

```
lib/
├── main.dart                  # 应用入口
├── app/                       # 应用层
│   ├── routes/                # 路由配置
│   ├── themes/                # 主题配置
│   └── config/                # 全局配置
├── features/                  # 功能模块
│   ├── auth/                  # 认证
│   ├── generate/              # 语音生成
│   │   ├── presentation/      # UI 层
│   │   │   ├── pages/
│   │   │   ├── widgets/
│   │   │   └── providers/
│   │   ├── domain/            # 业务层
│   │   │   ├── models/
│   │   │   ├── repositories/
│   │   │   └── usecases/
│   │   └── data/              # 数据层
│   │       ├── datasources/
│   │       ├── models/
│   │       └── repositories/
│   ├── library/               # 内容库
│   ├── settings/              # 设置
│   └── player/                # 播放器
├── core/                      # 核心功能
│   ├── api/                   # API 客户端
│   ├── database/              # 本地数据库
│   ├── utils/                 # 工具类
│   ├── constants/             # 常量
│   └── extensions/            # 扩展方法
└── shared/                    # 共享组件
    ├── widgets/               # 通用组件
    ├── models/                # 通用模型
    └── services/              # 通用服务
```

---

## 🎨 UI/UX 设计

### 设计原则

1. **简洁高效**：核心操作不超过 3 步
2. **即时反馈**：加载状态、进度提示
3. **手势优化**：滑动、长按、双击
4. **离线友好**：缓存内容、离线播放

### 主题配置

```dart
// 深色主题（主推）
ThemeData darkTheme = ThemeData(
  useMaterial3: true,
  colorScheme: ColorScheme.dark(
    primary: Color(0xFF2563EB),      // Trust Blue
    secondary: Color(0xFFF97316),    // Orange
    surface: Color(0xFF0A0E27),      // Dark Navy
    background: Color(0xFF121212),   // Dark Gray
  ),
  fontFamily: 'DM Sans',
  textTheme: TextTheme(
    displayLarge: TextStyle(fontFamily: 'Space Grotesk'),
    displayMedium: TextStyle(fontFamily: 'Space Grotesk'),
    headlineLarge: TextStyle(fontFamily: 'Space Grotesk'),
  ),
);

// 浅色主题
ThemeData lightTheme = ThemeData(
  useMaterial3: true,
  colorScheme: ColorScheme.light(
    primary: Color(0xFF2563EB),
    secondary: Color(0xFFF97316),
  ),
  fontFamily: 'DM Sans',
);
```

### 核心页面结构

#### 1. 首页（生成页）

```
┌──────────────────────────────────┐
│  [Logo]  [消息] [设置]           │ ← AppBar
├──────────────────────────────────┤
│                                  │
│  💬 输入文字，AI 生成语音        │ ← 提示卡片
│                                  │
│  ┌────────────────────────────┐ │
│  │ 在这里输入你想转换成语音    │ │ ← 文本输入
│  │ 的内容...                   │ │
│  │                             │ │
│  └────────────────────────────┘ │
│                                  │
│  音色选择  [小明 ▼]             │ ← 下拉选择
│  类型选择  [播客对话 ▼]         │
│                                  │
│  [🎙️ 生成语音]                  │ ← 主按钮
│                                  │
│  ──── 最近生成 ────              │
│  [项目1] [▶] [⋮]                │ ← 卡片列表
│  [项目2] [▶] [⋮]                │
│                                  │
└──────────────────────────────────┘
│ [首页] [库] [我的]               │ ← Bottom Nav
└──────────────────────────────────┘
```

#### 2. 内容库页

```
┌──────────────────────────────────┐
│  我的内容库                       │
│  [🔍 搜索] [📁 分类] [⋮]        │
├──────────────────────────────────┤
│  全部 | 播客 | 有声书 | 配音     │ ← Tabs
├──────────────────────────────────┤
│  ┌────────────────────────────┐ │
│  │ 📁 我的播客                │ │
│  │ 12 个文件 · 2.3GB          │ │
│  └────────────────────────────┘ │
│  ┌────────────────────────────┐ │
│  │ 🎵 AI 趋势讨论             │ │
│  │ 0:34 · 今天 14:25          │ │
│  │ [▶]  [📥]  [🗑️]           │ │
│  └────────────────────────────┘ │
└──────────────────────────────────┘
```

#### 3. 播放器页

```
┌──────────────────────────────────┐
│  [← ]  AI 趋势讨论  [⋮]          │
├──────────────────────────────────┤
│                                  │
│         🎵                       │ ← 可视化波形
│    ╱╲  ╱╲╱╲  ╱╲                │
│   ╱  ╲╱    ╲╱  ╲               │
│                                  │
│  ●─────────────○────── 0:34     │ ← 进度条
│  0:12                            │
│                                  │
│        [⏮] [⏸️] [⏭]             │ ← 播放控制
│                                  │
│  [🔁] [❤️] [📤] [⬇️]             │ ← 功能按钮
│                                  │
└──────────────────────────────────┘
```

#### 4. 设置页

```
┌──────────────────────────────────┐
│  设置                             │
├──────────────────────────────────┤
│  账户                             │
│  └ premium@aimake.cc              │
│  └ [Pro 会员] 续费至 2026-12     │
│                                  │
│  偏好设置                         │
│  └ 默认音色  [小明 ▼]            │
│  └ 默认类型  [播客 ▼]            │
│  └ 音频质量  [高质量 ▼]          │
│                                  │
│  存储与缓存                       │
│  └ 自动下载  [开]                │
│  └ 清理缓存  [2.3GB]             │
│                                  │
│  关于                             │
│  └ 版本 1.0.0                    │
│  └ 使用条款                       │
│  └ 隐私政策                       │
└──────────────────────────────────┘
```

---

## 🔧 核心功能模块

### 1. 语音生成模块

**流程**：

```
输入文字 → 参数配置 → 调用 API → 流式返回 → 本地缓存 → 播放
```

**关键代码**：

```dart
class GenerateAudioUseCase {
  final AudioRepository repository;

  Future<Stream<GenerateProgress>> execute(GenerateRequest request) async {
    // 1. 验证输入
    if (request.text.isEmpty) throw InvalidInputException();

    // 2. 调用 TTS API（支持流式）
    final stream = repository.generateAudio(
      text: request.text,
      voice: request.voice,
      type: request.type,
    );

    // 3. 返回进度流
    return stream.map((progress) {
      if (progress.isComplete) {
        // 保存到本地
        repository.saveToLocal(progress.audioFile);
      }
      return progress;
    });
  }
}
```

### 2. 音频播放器模块

**功能**：

- 播放、暂停、快进、快退
- 后台播放 + 锁屏控制
- 播放列表管理
- 睡眠定时器

**技术实现**：

```dart
// 使用 audio_service + just_audio
class AudioPlayerService {
  final AudioPlayer _player = AudioPlayer();
  final AudioHandler _handler = await AudioService.init(...);

  Future<void> play(AudioSource source) async {
    await _player.setAudioSource(source);
    await _player.play();

    // 更新通知栏
    _handler.playbackState.add(PlaybackState(
      playing: true,
      controls: [
        MediaControl.skipToPrevious,
        MediaControl.pause,
        MediaControl.skipToNext,
      ],
    ));
  }
}
```

### 3. 离线缓存模块

**策略**：

- 自动缓存最近 30 天的内容
- 支持手动下载
- 智能清理（LRU）

**存储结构**：

```
app_documents/
├── audio/                    # 音频文件
│   ├── podcast_123.mp3
│   └── audiobook_456.mp3
├── cache/                    # 临时缓存
└── database/                 # 数据库
    └── aimake.db
```

### 4. 同步模块

**同步内容**：

- 生成历史
- 收藏列表
- 播放进度
- 用户设置

**同步时机**：

- 应用启动
- 切换账号
- 手动触发
- 定期后台同步（WiFi）

---

## 🚀 开发计划

### Phase 1：MVP 核心功能（2 个月）

**Week 1-2：项目搭建**

- Flutter 项目初始化
- CI/CD 配置（GitHub Actions）
- 基础架构搭建
- UI 组件库

**Week 3-4：认证模块**

- 集成 Clerk SDK
- 登录/注册页面
- Token 管理
- 状态同步

**Week 5-6：生成功能**

- 文本输入页面
- 参数配置
- API 调用封装
- 进度显示

**Week 7-8：播放器**

- 音频播放器
- 播放控制
- 后台播放
- 锁屏控制

### Phase 2：完善体验（1.5 个月）

**Week 9-10：内容管理**

- 内容库页面
- 分类管理
- 搜索功能
- 批量操作

**Week 11-12：离线功能**

- 下载管理
- 缓存策略
- 离线播放
- 存储管理

**Week 13-14：设置与优化**

- 设置页面
- 主题切换
- 性能优化
- Bug 修复

### Phase 3：高级功能（1 个月）

**Week 15-16：高级特性**

- 播客脚本生成
- 多角色对话
- 语音克隆（Pro）
- 批量生成

**Week 17-18：发布准备**

- 测试覆盖
- 应用签名
- Store 提交
- 文档完善

---

## 📦 发布策略

### App Store（iOS）

**准备工作**：

1. Apple Developer 账号（$99/年）
2. 应用图标（1024x1024）
3. 应用截图（6.7"、6.5"、5.5" iPhone）
4. 隐私政策 URL
5. 应用描述（多语言）

**审核要点**：

- ✅ 不违反 App Store 审核指南
- ✅ 提供测试账号
- ✅ 功能完整可用
- ✅ 隐私说明清晰

### Google Play（Android）

**准备工作**：

1. Google Play 开发者账号（$25 一次性）
2. 应用图标（512x512）
3. 应用截图（手机 + 平板）
4. 功能图片
5. 应用描述

**发布渠道**：

- **内部测试**：开发团队
- **封闭测试**：早期用户（100 人）
- **开放测试**：公开 Beta（无限制）
- **正式发布**：全球上线

---

## 💰 商业化方案

### 定价策略

| 版本       | 月费 | 年费           | 功能                                      |
| ---------- | ---- | -------------- | ----------------------------------------- |
| **免费版** | $0   | $0             | 10 分钟/月、5 种音色、MP3 导出            |
| **Pro**    | $19  | $190 (省 $38)  | 300 分钟/月、所有音色、播客生成、WAV 导出 |
| **Team**   | $99  | $990 (省 $198) | 无限分钟、5 成员、API 接入、优先支持      |

### 应用内购买（IAP）

**iOS（StoreKit 2）**：

```dart
// 订阅配置
final products = [
  Product(
    id: 'pro_monthly',
    price: '\$19.00',
    period: SubscriptionPeriod.month,
  ),
  Product(
    id: 'pro_yearly',
    price: '\$190.00',
    period: SubscriptionPeriod.year,
  ),
];
```

**Android（Google Play Billing）**：

```dart
// 使用 in_app_purchase 插件
final ProductDetailsResponse response =
  await InAppPurchase.instance.queryProductDetails({
    'pro_monthly',
    'pro_yearly',
  });
```

### 收入分成

- App Store：30%（首年）→ 15%（次年及以后）
- Google Play：15%（首个 $1M）→ 30%（超过部分）

---

## 🔐 安全与隐私

### 数据加密

**传输层**：

- HTTPS/TLS 1.3
- Certificate Pinning

**存储层**：

```dart
// 敏感数据加密存储
final storage = FlutterSecureStorage();
await storage.write(key: 'auth_token', value: token);
```

### 隐私合规

**GDPR / CCPA**：

- ✅ 数据最小化
- ✅ 用户同意
- ✅ 数据删除
- ✅ 隐私政策

**iOS 隐私清单**（Privacy Nutrition Label）：

```yaml
数据收集:
  - 联系信息: 邮箱（用于账户管理）
  - 使用数据: 生成历史（用于服务改进）
  - 诊断数据: 崩溃日志（用于 Bug 修复）

数据用途:
  - 不会出售给第三方
  - 不会用于广告定向
```

---

## 📊 性能指标

### 关键指标（KPIs）

| 指标           | 目标   | 测量方法             |
| -------------- | ------ | -------------------- |
| **启动时间**   | < 1.5s | Firebase Performance |
| **生成延迟**   | < 3s   | 自定义埋点           |
| **播放卡顿率** | < 0.5% | 音频事件监听         |
| **崩溃率**     | < 0.1% | Sentry / Crashlytics |
| **日活跃率**   | > 20%  | Firebase Analytics   |

### 性能优化

**包体积优化**：

- 使用 `--split-per-abi` 生成多架构包
- 移除未使用的资源
- 图片压缩（WebP）
- 代码混淆

**网络优化**：

- 请求合并
- 数据压缩（Gzip）
- CDN 加速
- 智能重试

---

## 🧪 测试策略

### 单元测试

```dart
// 使用 flutter_test
test('GenerateAudioUseCase should return stream', () async {
  final useCase = GenerateAudioUseCase(mockRepository);
  final stream = await useCase.execute(request);

  expect(stream, emitsInOrder([
    isA<GenerateProgress>().having((p) => p.progress, 'progress', 0.0),
    isA<GenerateProgress>().having((p) => p.isComplete, 'complete', true),
  ]));
});
```

### 集成测试

```dart
// 使用 integration_test
testWidgets('Generate audio flow', (tester) async {
  await tester.pumpWidget(MyApp());

  // 输入文字
  await tester.enterText(find.byType(TextField), '测试文字');

  // 点击生成
  await tester.tap(find.text('生成语音'));
  await tester.pumpAndSettle();

  // 验证播放器出现
  expect(find.byType(AudioPlayer), findsOneWidget);
});
```

### 覆盖率目标

- 单元测试：> 80%
- 集成测试：核心流程 100%
- UI 测试：关键页面 100%

---

## 🔗 集成服务

### 必需服务

| 服务         | 用途             | 成本               |
| ------------ | ---------------- | ------------------ |
| **Firebase** | 分析、崩溃、推送 | 免费（Spark 计划） |
| **Sentry**   | 错误监控         | $26/月（Team）     |
| **Clerk**    | 用户认证         | 已集成（Web 同步） |
| **Stripe**   | 支付订阅         | 2.9% + $0.30/笔    |

### 可选服务

| 服务          | 用途                 | 优先级 |
| ------------- | -------------------- | ------ |
| **OneSignal** | 推送通知（替代 FCM） | 低     |
| **Mixpanel**  | 高级分析             | 中     |
| **Intercom**  | 客服支持             | 中     |

---

## 📱 平台特性

### iOS 特有功能

- **Apple Sign In**：必须提供
- **Siri Shortcuts**：语音快捷操作
- **CarPlay**：车载播放
- **Watch App**：Apple Watch 控制

### Android 特有功能

- **Material You**：动态颜色主题
- **Widget**：桌面小组件
- **Quick Settings Tile**：快速设置
- **Android Auto**：车载集成

---

## 🚢 部署与发布

### CI/CD 流程

```yaml
# .github/workflows/flutter-ci.yml
name: Flutter CI

on:
  push:
    branches: [main, develop]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: subosito/flutter-action@v2
      - run: flutter pub get
      - run: flutter test
      - run: flutter analyze

  build-android:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: subosito/flutter-action@v2
      - run: flutter build apk --release
      - run: flutter build appbundle --release

  build-ios:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v3
      - uses: subosito/flutter-action@v2
      - run: flutter build ios --release --no-codesign
```

### 版本发布流程

1. **开发分支**：feature/\* → develop
2. **测试版本**：develop → release/\*
3. **正式发布**：release/\* → main
4. **标签创建**：git tag v1.0.0
5. **商店提交**：自动上传 TestFlight / Internal Testing

---

## 📚 相关文档

- [Flutter 官方文档](https://docs.flutter.dev/)
- [App Store 审核指南](https://developer.apple.com/app-store/review/guidelines/)
- [Google Play 政策中心](https://play.google.com/about/developer-content-policy/)
- [Stripe 移动端集成](https://stripe.com/docs/mobile)

---

## ✅ 待办事项

### MVP 前置

- [ ] 注册 Apple Developer 账号
- [ ] 注册 Google Play 开发者账号
- [ ] 设计应用图标（1024x1024）
- [ ] 准备应用截图（iOS + Android）
- [ ] 编写隐私政策
- [ ] 编写服务条款

### 开发阶段

- [ ] 搭建 Flutter 项目
- [ ] 集成 Clerk SDK
- [ ] 集成 Stripe SDK
- [ ] 实现核心 TTS 功能
- [ ] 实现音频播放器
- [ ] 完成 UI/UX 设计

### 测试阶段

- [ ] 单元测试覆盖
- [ ] 集成测试
- [ ] 内部 Beta 测试
- [ ] 公开 Beta 测试

### 发布阶段

- [ ] App Store 提交
- [ ] Google Play 提交
- [ ] 营销素材准备
- [ ] 官网更新

---

**最后更新**: 2026-01-10 **版本**: 1.0 **状态**: 规划中

**预计开发周期**: 4.5 个月（MVP） **预计成本**: $20K - $30K（人力 + 账号 + 服务）
